# compile-and-check.md

This rule describes how to check the code.

## Guidelines

- To test Rust code, use the `cargo nextest` command. DO NOT use `cargo test`.
- To check Rust code, use the `cargo check` command. Check code prior to telling the user that the task is complete.
